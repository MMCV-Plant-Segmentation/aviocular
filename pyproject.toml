[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "aviocular"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = ["flask"]

[project.scripts]
aviocular = "aviocular.cli:main"
